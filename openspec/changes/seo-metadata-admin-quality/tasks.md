## 1. SEO contract

- [x] Уточнить prompt генерации SEO-пакета: смысловая связь с H1/body,
  орфография, естественный русский заголовок и запрет на новые факты.
- [x] Спроектировать и добавить `seo_review_report` с field, severity, rule,
  evidence и length для RU/EN.
- [x] Добавить миграцию и API-модели для хранения и возврата SEO-отчета.

## 2. Publishing and admin

- [x] Встроить проверку при генерации и при ручном сохранении SEO-полей.
- [x] Блокировать Publish только при SEO blocking-ошибках и отображать точную
  причину в UI.
- [x] Добавить RU/EN SERP-превью, счетчики, OG-поля и список замечаний на
  странице черновика.

## 3. Tests and rollout

- [x] Добавить тесты на несоответствие H1/SEO-title, орфографию,
  источник-обусловленные числа и неблокирующие длины.
- [x] Добавить API/UI-тесты на сохранение, повторную проверку и запрет Publish.
- [ ] Выполнить `ruff` и `pytest` через `uv` (локально `uv` отсутствует), `git diff --check` и строгую
  OpenSpec-проверку; развертывать через PR в dev, затем отдельный релизный PR
  в main.
