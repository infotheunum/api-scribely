## 1. Prompt version

- [x] Добавить v16 appendix с правилами стиля, заголовков, аббревиатур,
  терминологии и финальной вычитки.
- [x] Реализовать append-only создание draft v16 из ACTIVE prompt и скрипт
  запуска; не активировать кандидата автоматически.

## 2. Quality gate and review

- [x] Дополнить контракт фактчекера языковой и терминологической проверкой.
- [x] Добавить `editorial_review_flags` в схему report и отобразить их в
  карточке ревью черновика.

## 3. Tests and rollout

- [x] Добавить тесты сохранения template/status активной версии и состава v16.
- [x] Добавить тесты на «Так,», «тем не менее», аббревиатуры, заголовки и
  словарь терминов.
- [ ] Выполнить `ruff` и `pytest` через `uv` (локально `uv` отсутствует), `git diff --check` и строгую
  OpenSpec-проверку; после ревью создать кандидата в dev, сравнить с active и
  отдельно активировать в production.
